{
  "name": "agent-ui-kit-demo",
  "private": true,
  "type": "module",
  "description": "Съёмка ролика для README. Держится отдельно, чтобы у самого пакета не появилось зависимостей.",
  "scripts": {
    "record": "playwright test",
    "cut": "node cut.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "playwright-recast": "^0.21.0"
  }
}
