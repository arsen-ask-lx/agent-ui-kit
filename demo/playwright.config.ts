import { defineConfig } from "@playwright/test";

// Съёмка ролика, а не проверка. Отсюда и настройки: один рабочий поток,
// без повторов, с записью следа — из него recast и собирает видео.
export default defineConfig({
  testDir: ".",
  workers: 1,
  retries: 0,
  // Ролик идёт около минуты, и это не признак зависания: стандартные
  // тридцать секунд рассчитаны на проверку, а здесь съёмка с паузами
  // на прочтение подписей.
  timeout: 180_000,
  use: {
    baseURL: "http://localhost:5177",
    // ⚠️ СНИМАЕМ ВДВОЕ БОЛЬШЕ, ЧЕМ ВЫДАЁМ, И ЭТО ГЛАВНОЕ ПРО РЕЗКОСТЬ.
    // Готовый ролик — 1080p, наезд около полутора крат: значит каждый
    // увеличенный кадр вырезается из 1440p один к одному, без растягивания.
    // Первый ролик писался в 720p и растягивался до 1080p с двукратным
    // наездом — увеличение вчетверо, и на кадре не читалось ничего.
    //
    // Плотность здесь единичная нарочно: при двойной содержимое окна
    // ложится в угол холста записи, проверено — кадр уехал влево.
    viewport: { width: 2560, height: 1440 },
    trace: "on",
    video: { mode: "on", size: { width: 2560, height: 1440 } },
  },
  // Пример поднимается сам. Если он уже запущен — берём запущенный.
  webServer: {
    command: "npm run example -- --port 5177 --strictPort",
    cwd: "..",
    url: "http://localhost:5177",
    reuseExistingServer: true,
    timeout: 120_000,
  },
});
